

## Concepts covered
- useRef
- React Router
- Component Lifecycle methods
    - `ComponentDidMount()`
    - `ComponentWillUpdate()`
    - `ComponentDidUnmount()`
- Firebase for persisting state
- `localStorage` for persisting state 